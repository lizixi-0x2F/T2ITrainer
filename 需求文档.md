
## 主要需求
阅读整个项目环境，熟悉整个项目，列出项目的实现清单给我，让我来看看项目的结构和实现，以及罗列出这个项目的显卡加速代码（mps、cuda etc）。
已知这个项目是一个关于flux等扩散模型的训练UI，但是他只能在单显卡上面进行训练，我的需求是将它改为多显卡并行版本。同时我的机器是RTX4090，请为这个机器定制优化代码
## 技术栈调研
1. nn.DataParallel简单方便的 nn.DataParallel
2. torch.distributed 使用 torch.distributed 加速并行训练
3. torch.multiprocessing 使用 torch.multiprocessing 取代启动器
4. apex 使用 apex 再加速
5. horovod horovod 的优雅实现
6. 注：分布式 evaluation

## 基本步骤
分布式策略改进
FSDP完全整合：用PyTorch的FSDP替代DDP，支持更大模型
渐进式批处理大小：训练开始时使用小批量，稳定后增加
混合精度训练策略：不同组件使用不同精度
通信优化：使用NCCL调优，减少节点间通信瓶颈
梯度压缩：实现PowerSGD等算法减少通信开销
评估每个显卡并行方案的性能，可视化。如果不支持进行版本更迭、替换方案，选出最好的性能方案，同时适配多端机器能够进行部署

训练流程优化
样本难度调度：先训练简单样本，逐步引入复杂样本
自适应学习率：根据每个GPU的梯度统计自动调整学习率
模型预热阶段：特殊的预热调度提高稳定性
混合并行策略：同时使用数据并行和模型并行